{
  "name": "electron-terminal",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . Terminal --platform=win32 --arch=x64 --out=release-builds --overwrite"
  },
  "devDependencies": {
    "electron": "^32.0.1",
    "electron-packager": "^17.1.2"
  }
}
